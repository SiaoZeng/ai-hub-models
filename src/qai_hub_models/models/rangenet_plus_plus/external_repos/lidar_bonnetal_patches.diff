# Replaces deprecated imp module with importlib and converts to relative imports.
#
diff --git a/train/tasks/semantic/modules/segmentator.py b/train/tasks/semantic/modules/segmentator.py
index 20c9539..f94bb10 100644
--- a/train/tasks/semantic/modules/segmentator.py
+++ b/train/tasks/semantic/modules/segmentator.py
@@ -1,12 +1,22 @@
 #!/usr/bin/env python3
 # This file is covered by the LICENSE file in the root of this project.

-import imp
+import importlib.util
+from pathlib import Path
+
 import torch
 import torch.nn as nn
 import torch.nn.functional as F
-from tasks.semantic.postproc.CRF import CRF
-import __init__ as booger
+from ..postproc.CRF import CRF
+
+_TRAIN_PATH = str(Path(__file__).resolve().parents[3])
+
+
+def _load_source(name, path):
+    spec = importlib.util.spec_from_file_location(name, path)
+    mod = importlib.util.module_from_spec(spec)
+    spec.loader.exec_module(mod)
+    return mod


 class Segmentator(nn.Module):
@@ -19,9 +29,9 @@ class Segmentator(nn.Module):
     self.strict = False

     # get the model
-    bboneModule = imp.load_source("bboneModule",
-                                  booger.TRAIN_PATH + '/backbones/' +
-                                  self.ARCH["backbone"]["name"] + '.py')
+    bboneModule = _load_source("bboneModule",
+                               _TRAIN_PATH + '/backbones/' +
+                               self.ARCH["backbone"]["name"] + '.py')
     self.backbone = bboneModule.Backbone(params=self.ARCH["backbone"])

     # do a pass of the backbone to initialize the skip connections
@@ -35,9 +45,9 @@ class Segmentator(nn.Module):
       self.backbone.cuda()
     _, stub_skips = self.backbone(stub)

-    decoderModule = imp.load_source("decoderModule",
-                                    booger.TRAIN_PATH + '/tasks/semantic/decoders/' +
-                                    self.ARCH["decoder"]["name"] + '.py')
+    decoderModule = _load_source("decoderModule",
+                                 _TRAIN_PATH + '/tasks/semantic/decoders/' +
+                                 self.ARCH["decoder"]["name"] + '.py')
     self.decoder = decoderModule.Decoder(params=self.ARCH["decoder"],
                                          stub_skips=stub_skips,
                                          OS=self.ARCH["backbone"]["OS"],
diff --git a/train/tasks/semantic/postproc/CRF.py b/train/tasks/semantic/postproc/CRF.py
index 24f52d1..ff92b7e 100644
--- a/train/tasks/semantic/postproc/CRF.py
+++ b/train/tasks/semantic/postproc/CRF.py
@@ -2,11 +2,9 @@
 # This file is covered by the LICENSE file in the root of this project.

 import numpy as np
-from scipy import signal
 import torch
 import torch.nn as nn
 import torch.nn.functional as F
-import __init__ as booger


 class LocallyConnectedXYZLayer(nn.Module):
